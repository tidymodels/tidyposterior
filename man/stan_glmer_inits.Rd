% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialization.R
\name{stan_glmer_inits}
\alias{stan_glmer_inits}
\title{Data-based starting values for Bayesian resampling models}
\usage{
stan_glmer_inits(
  formula,
  data,
  seed = sample.int(10^5, 1),
  jitter = 0.1,
  blocks = c("fixed", "aux", "ranef")
)
}
\arguments{
\item{formula}{A two-sided formula with a single numeric outcome, fixed
effects, and at least one random effect term (in the \code{lme4::lmer()}
syntax used by \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}). This should be the same formula
that will be used to fit the model. See Details for the supported
structures.}

\item{data}{A data frame with the columns used in \code{formula}; the same data
that will be given to \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}. No missing values are
allowed in the columns that are used.}

\item{seed}{A single integer controlling the per-chain random jittering.}

\item{jitter}{A single non-negative number for how much the starting
values are perturbed for each chain. Location parameters are shifted by
Gaussian noise with this standard deviation (on their natural,
standardized scale) and scale parameters are multiplied by a log-normal
factor with this standard deviation. A value of zero gives every chain
identical starting values (not recommended for diagnostics).}

\item{blocks}{A character vector for which groups of parameters get
data-based starting values: \code{"fixed"} (the intercept and fixed effect
coefficients), \code{"aux"} (the residual standard deviation), and/or
\code{"ranef"} (the random effect values and their covariance parameters).
Parameters outside of \code{blocks} retain \pkg{rstan}'s random initialization.}
}
\value{
A function of \code{chain_id} (with class \code{"stan_glmer_inits"}) that
returns a named list of starting values for the model's underlying Stan
parameters. \pkg{rstan} invokes it once per chain. The unjittered values
are stored in the \code{"inits"} attribute.
}
\description{
\code{\link[=stan_glmer_inits]{stan_glmer_inits()}} computes starting values for the Bayesian hierarchical
models used by \code{\link[=perf_mod]{perf_mod()}} from group-level sample statistics. The results
can be passed to the \code{init} argument of \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}} (directly
or through \code{perf_mod(..., init = )}) and can substantially shorten warmup
for models that are slow to converge, especially those with heterogeneous
group variances.
}
\details{
The function is designed for the model structures created by \code{\link[=perf_mod]{perf_mod()}}:

\preformatted{
  statistic ~ model + (model + 0 | id)   # heterogeneous variances
  statistic ~ model + (1 | id)           # homogeneous variances
  statistic ~ model + (1 | id2/id)       # homogeneous, repeated CV
}

Other formulas with a fixed-effect intercept and factor-based random
effect terms may also work but are not tested. Note that the
heterogeneous-variance model with nested resamples
(\code{statistic ~ model + (model + 0 | id2/id)}) cannot be fit by
\code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}} for balanced resampling data because the number
of random effects equals the number of observations.

Starting values are estimated with ordinary least squares: the fixed
effects come from a linear model, the random effect values are per-level
summaries of its residuals, and their covariance matrix initializes the
correlation/variance decomposition used by \pkg{rstanarm}. The residual
standard deviation is estimated after removing additive grouping-factor
effects and is floored at 1\\% of the outcome's standard deviation; when
there is one observation per model/resample combination, this residual
scale is confounded with the random effects, so the value is a heuristic.

The computations assume the model will be fit with \code{family = gaussian()}
(the \code{\link[=perf_mod]{perf_mod()}} default), no case weights, \code{QR = FALSE}, and the default
\pkg{rstanarm} priors (which automatically rescale using the data). With
non-default priors, the starting values are still usable but are no longer
on the intended scale.

The \strong{same} \code{formula} and \code{data} (after any outcome transformation) must
be given to this function and to \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}; otherwise the
starting values may silently correspond to the wrong random effect levels.
The easiest way to use these starting values with \code{\link[=perf_mod]{perf_mod()}} is its
\code{initialize = TRUE} argument, which computes and applies them
automatically. To call this function manually (e.g., to control \code{blocks}
or \code{jitter}), recreate the internal data format of \code{\link[=perf_mod]{perf_mod()}}: the
resampling results are stacked into a data frame with a \code{statistic} column
(with the \code{transform$func} applied), a \code{model} column, and the resampling
id column(s) (see the example below).

Data-based starting values do not uniformly reduce sampling time. For
models that already converge quickly they can add modest overhead, while
for slow-converging fits (e.g., \code{hetero_var = TRUE}) they can remove the
occasional very slow chain that started in a poor region of the parameter
space. They also tend to reduce the number of divergent transitions, but
divergences caused by the model geometry require increasing
\code{adapt_delta}, not better starting values.
}
\examples{
data(precise_example)

# The data in the format used by perf_mod():
roc_data <-
  precise_example |>
  dplyr::select(id, dplyr::contains("ROC")) |>
  tidyr::pivot_longer(
    cols = c(-id),
    names_to = "model",
    values_to = "statistic"
  ) |>
  dplyr::mutate(model = gsub("_ROC", "", model))

inits <- stan_glmer_inits(
  statistic ~ model + (model + 0 | id),
  data = roc_data,
  seed = 1
)
inits

# The starting values for the second chain:
str(inits(2))

\donttest{
fit <- rstanarm::stan_glmer(
  statistic ~ model + (model + 0 | id),
  data = roc_data,
  init = inits,
  refresh = 0,
  seed = 2
)
}
}
\seealso{
\code{\link[=perf_mod]{perf_mod()}}, \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}
}
\keyword{internal}
