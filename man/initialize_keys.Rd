% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_to_leader.R
\name{initialize_keys}
\alias{initialize_keys}
\title{Create a Template of Model Labels}
\usage{
initialize_keys(x)
}
\arguments{
\item{x}{An object produced by \code{\link[=perf_mod]{perf_mod()}}.}
}
\value{
A tibble with one row per model and two columns: \code{model}, holding
the model names recorded in \code{x}, and \code{label}, initialized to the same
values.
}
\description{
\code{\link[=compare_to_leader]{compare_to_leader()}} can relabel models for plotting through its \code{key}
argument. \code{initialize_keys()} builds a template for that argument with one
row per model, ready to have its \code{label} column edited.
}
\details{
Leave the \code{model} column alone. Those names are what
\code{\link[=compare_to_leader]{compare_to_leader()}} joins on, and every model in \code{x} has to be
represented, so editing or dropping them will produce an error. Edit the
\code{label} column to whatever should appear on the plot.

Rows may be reordered and extra columns may be added; both are ignored.
Labels do not have to be unique.
}
\examples{
\dontshow{if (rlang::is_installed(c("parsnip", "yardstick"))) withAutoprint(\{ # examplesIf}
library(parsnip)
library(rsample)
library(workflowsets)

set.seed(1)
folds <- vfold_cv(mtcars, v = 5)

\donttest{
mpg_models <-
  workflow_set(
    preproc = list(
      small = mpg ~ wt,
      medium = mpg ~ wt + hp,
      large = mpg ~ .
    ),
    models = list(lm = linear_reg())
  ) |>
  workflow_map("fit_resamples", resamples = folds, seed = 2)

set.seed(4321)
mpg_post <- perf_mod(mpg_models, metric = "rmse", refresh = 0, chains = 2)

mpg_keys <- initialize_keys(mpg_post)
mpg_keys

# Edit the labels, then pass the result along:
mpg_keys$label <- c("1 predictor", "2 predictors", "all predictors")

mpg_post |>
  compare_to_leader(size = 0.5, key = mpg_keys, seed = 2) |>
  autoplot()
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=compare_to_leader]{compare_to_leader()}}
}
